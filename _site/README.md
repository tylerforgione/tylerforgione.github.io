# tylerforgione.github.io

Personal academic portfolio site built with Jekyll and hosted on GitHub Pages.

## Local dev
If you want to preview locally:

```bash
bundle install
bundle exec jekyll serve